# Standard workflow

Start with the smallest outcome that tests the product assumption. Inspect existing assets, state explicit assumptions, plan a vertical slice, implement it, validate behavior, and summarize evidence. Keep learning proposals reviewable and never modify core persona rules automatically.
