// Copy to config.js and replace these public deployment values before hosting.
export const MAVECODE_AUTH_CONFIG = Object.freeze({
	appsScriptUrl: "https://script.google.com/macros/s/REPLACE_WITH_DEPLOYMENT_ID/exec",
	googleClientId: "REPLACE_WITH_WEB_CLIENT_ID.apps.googleusercontent.com",
})
